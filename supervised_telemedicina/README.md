# supervised_telemedicina

Placeholder del nuovo progetto di telemedicina con approccio **supervised
learning** (MLP).

> Stato: **Fase 0** — solo scheletro del progetto. Nessuna implementazione
> funzionale. Il progetto legacy (rule-based / Q-learning) è archiviato in
> `../archive/legacy_qtable_rule_based/` e **non deve essere importato** dal
> nuovo runtime.

## Struttura

- `main.py` — entry point (placeholder non funzionale).
- `src/telemedicina_supervised/` — package principale con i moduli
  `agents`, `database`, `ml`, `models`, `safety`, `services`, `utils`.
- `training/` — script di training (da implementare).
- `tests/` — test (da implementare).
- `requirements.txt` — dipendenze Python.

## Roadmap

- [ ] Fase 0: scheletro del progetto (corrente)
- [ ] Implementazione MLP e business logic (fasi successive)